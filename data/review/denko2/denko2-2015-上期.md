# denko2-2015-上期 レビューノート

- 素材: スキャンPDF（`meta.json` の `textPdf: false`）。`questions_raw.json` の OCR 草稿は誤りが多かったため，
  **問1〜問50 のすべてを `cells/qNN_row.png` で目視確認**して起こし直した。配線図面は `pages/p15.png`（コンビニ／店舗の平面図＋電灯・動力分電盤結線図）。
- 解答キーは `answers.json`（`answerSource: pdf`）。`check_answers.py` で 50 問すべて一致，欠番なし。

## OCR が読めず画像で判断した箇所（全問だが，特に注意した点）

- 問1「端子a－b間」が `端子ａ－し間`，問2「リアクタンス」が `リ７クタンス`，`X_L＝3 Ω` が `Ａ－３ｃ`，
  問5 の選択肢（分数式）はテキストがほぼ壊れていたため，画像から `E/(2R)` `E/(√3R)` `E/R` `3E/(2R)` に起こした。
  分数は 1 行表記にするため括弧を補った（式の意味は変えていない）。
- 問7 の `直径1.6mIの` → 「直径1.6 mm の」，`電流減小係数は０70` → 「電流減少係数は0.70」。
- 問9 は OCR で 1 行目が欠落していた（`で保護された低圧屋内幹線から…` から始まっていた）。画像から全文を復元。
- 問10・問23・問25 は選択肢が図なので `choicesFigure` を付け，本文は内容を短く要約した（DATA_FORMAT の「条件の組合せ」型）。
- 問41〜問50 の選択肢はすべて写真・図。写真の識別そのものが答えになる問は `["写真イ", …]`，
  組合せを答える問（43・45・46）は内容を短文で記した。

## 図の切り出し

`cells/qNN_figure.png` が無い回なので，`scripts/crop_figure.py` で `custom/qNN_fig.png` を作成し，すべて Read で目視確認した。

- 問1・問4・問5・問16・問17・問18・問23 は `--auto` の結果をそのまま採用（問23 のみ `--y0 0.462 --y1 0.85` に調整。
  自動だと図の下半分しか入らず，本文の最終行も混じったため）。
- 問2（`--y0 0.455`）・問6（`--y0 0.325`）・問9（`--y0 0.44`）は `--auto` だと図の上端の「10 A」「100 A」が切れたので比率指定でやり直した。
- 配線図問題（31〜50）は `sharedFigure` に `p15.png` を指定。

## `note` を付けた問

- **問28**（電気の保安に関する法令）: 2023年3月の電気事業法改正で「一般用電気工作物等（＝一般用電気工作物＋小規模事業用電気工作物）」
  の区分ができたことを `note` に記載。誤答肢ニが誤りである理由（分類を定めるのは電気用品安全法ではなく電気事業法）は改正の前後で変わらないため `retired` にはしていない。
- **問29**（電気工事士でなければ従事できない作業）: 問題文の「一般用電気工作物」が現行では「一般用電気工作物等」である旨を `note` に記載。
  選択肢の正誤は変わらないため `active` のまま。
- `retired` にした問はない。

## calc にした問（3問）

- **問3**（電線の抵抗の比較）: 直径は 1.6/3.2 mm に固定し，長さ L_A・L_B を振る。
  `accept` で答えが 2〜6 の整数になる組合せだけ採用（12 通り）。2.0/4.0 mm の組も試したが，
  4.0 mm が実在の電線サイズとして不自然なので外した。
- **問7**（管内の許容電流）: 直径 1.6/2.6 mm × 本数と電流減少係数（3本以下0.70／4本0.63／5〜6本0.56）。
  `accept` で本数と係数の整合を強制。2.0 mm は `{d}` の置換が「2 mm」と表示されてしまうため除外した（仕様上 `String(値)` で置換されるため）。
- **問8**（幹線の太さを決める電流）: 定格電流・台数・需要率を振り，I_M ≤ 50 A で1.25倍・超過で1.1倍を式に入れた（19通り）。
- 問1・問2・問4・問5・問6・問9 は図の中に数値が描き込まれているため OPUS_TASK §4 に従い `static` のままとした。

## 新規グループ（`denko2-2015-上期.groups.json`）

- `g1-sanso-dansen-denryu`（三相回路の断線後に流れる電流）— 問5。既存の `g1-sanso-y-dansen` は Y結線の断線が対象で，
  本問は Δ負荷＋電源線断線を単相回路に読み替える型なので分けた。`relatedGroups` で関連付け済み。
- `g6-duct-shashin`（線ぴ・ダクト類の図記号と写真）— 問48。LD（ライティングダクト）の傍記から実物を選ぶ型で，
  既存の `g6-zairyou-shashin` は分野3，`g6-kigou-haisen-houhou` は線種の図記号なのでどちらにも収まらなかった。

## category / groupId で迷った問

- **問13**（コードの許容電流）: 出題位置は機器・材料の並びだが内容は許容電流なので分野2・`g2-cord-kyoyou-denryu`（2026年版と同じ扱い）。
- **問20**（進相コンデンサの接続場所）: 施工方法寄りにも読めるが，既存 `g3-shinso-condenser` が分野3なのでそれに合わせた（2026年版も分野3）。
- **問26**（接地抵抗計に関する記述）: 3極法の手順ではなく計器の性質を問う問題だが，`g5-setti-teikou-sokutei` に含めた。
- **問41 と問44** はどちらも `g6-bubun-kouji-zairyou`。前者は引込線引留具，後者は接地工事の材料・工具で知識は別だが，
  グループ定義の summary が両方を明示的に含んでいるためまとめた。分けたほうがよければ後で分割してほしい。

## 自信が持てなかった箇所（次工程で重点確認をお願いしたい）

1. **問41 の誤答肢ロの正体**。写真ロは「先端がフック状の磁器製支持具（下部にコイル状の金具と先のとがった打込み部）」で，
   正式な名称を特定できなかったため，解説では名称を書かず形状と用途（引き留めには使わない）だけを述べている。
   イ＝低圧ピンがいし，ニ＝玉がいし と判断したが，これも写真からの推定。
2. **問42 の図記号の読み**。⑫ は「黒く塗った四角の中に，丸で囲んだ S」。解説では
   「丸は電流計を内蔵することを示す＝電流計付箱開閉器」と説明した。JIS C 0303 の該当表記を確認していないので裏取りが必要。
   なお誤答肢ニは「COVER KNIFE SWITCH 3P 15A」と銘板にあるカバー付ナイフスイッチで，これも開閉器の一種である点は解説に明記した。
3. **問39 の取付け場所の判定根拠**。⑨ の図記号は塗りつぶしがなく壁線にも接していないので天井面と判断した（公式解答とも一致）。
   解説では「床面・二重床面はそれぞれ別の付加記号で区別する」と書くにとどめ，具体的な床面記号の形は書いていない。
4. **問43・問45・問46 の複線図の読み**。⑮（2つのジョイントボックス間）を
   「接地側1本＋非接地側1本＋3路スイッチの渡り線2本の計4本」と読み，
   その結果 ⑬ は小スリーブ4個，⑯ は 2本用3個・3本用1個・4本用1個 となる。
   3問の公式解答（イ／ロ／ロ）がこの1つの読みですべて整合したので確度は高いと考えているが，
   解説文で「照明3か所」「2方向の点滅器」と具体化した部分は図面の読み取りに依存している。
5. **問34（④の最少電線本数）**。手洗場側のボックスへ送るのは電源2本だけ，と判断した（公式解答イ＝2 と一致）。
   ④の指す配線が上下2つのジョイントボックス間であることは拡大して確認済み。
6. **問50** で「配線図に使われているコンセント」を写真イ（抜け止め形1口）と判断した根拠は，
   カウンタ付近の「LK」単独傍記のコンセント。他の LK は「LK EET WP」と併記されており写真ハ（接地極付抜け止め形2口）とは一致しない，
   という消去法も併用している。

## 仕様への提案

- `calcTemplate` の `{param}` 置換が `String(値)` のため，`2.0` が「2」と表示される。
  実試験の表記（2.0 mm，0.70）に合わせるには，パラメータごとに表示桁を指定できる仕組み（例 `paramDigits: {"d": 1}`）があると，
  電線の太さや係数をそのまま振れて calc 化できる問題が増える。今回は 2.0 mm を候補から外して回避した。
- 選択肢が複数行になる問（問15・問29 の a/b 形式など）は `\n` で改行して `Md` に任せているが，
  実試験の見た目に合わせるなら「複数行の選択肢」を明示的に扱える型があってもよい。
- パーセント記号は本ファイルでは半角 `%`（単位は半角というルールに従った）。
  2026年版は問題文で全角 `％`，解説で半角 `%` が混在しているので，どちらかに統一する方針を決めたほうがよい。

## 検証（第2工程）

### 機械的チェック
- `check_answers.py` → 50問，欠番なし，不一致なし。`validate-data.ts` → errors 0 / warnings 0。
- 分野分布は 1:5 2:6 3:8 4:4 5:4 6:20 7:3。目安（3:7 4:6 6:19）とはずれるが，
  全50問の `category` は `groups.json` の各グループの `category` と完全に一致しており，
  正準タクソノミ側の定義に従った結果なので変更しなかった。
- calc 3問（3・7・8）は `allValidCalcs` で 12 / 6 / 19 通り生成され，値域も実試験相当。
  `original` の値（3: 1.6/3.2/20/40，7: 1.6/3本/0.70，8: 12 A/5台/80 %）は原文と一致。

### 画像との照合
`cells/qNN_row.png` を**全50問**開いて `stem`／`choices` を原文と突き合わせた。
配線図は `pages/p15.png` を全体＋拡大10か所（凡例・カウンタ周辺・手洗場・事務所・
両分電盤結線図・回路番号マーカー）で確認した。公開済みの `figures/` 24点もすべて表示確認。

- **転記の誤りは見つからなかった**（Ω/W，φ，mm²，数値，「不適切／誤っている」等の否定条件，
  ただし書き，選択肢の取り違えのいずれも問題なし）。図の切り出しも欠けや本文の写り込みなし。
- 問33 の ③ は**二重丸のⓔ＝単相200 V回路**（凡例で確認）であり，解説の記述は正しい。
  一方 問43 の ⑬ 脇のⓔは**一重丸＝単相100 V回路**で，こちらも解説の白／黒の呼び方と整合する。

### 見つけて直した誤り（7か所）

1. **問43 の `whyCorrect` が複線図の読み違い**（実質的な誤り）。
   「左右に4本ずつ入り，いずれも1.6 mm×2本の接続なので圧着マークはすべて ○」と書かれていたが，
   ⑬のボックスには **電灯分電盤（回路ⓔ）からの2本＋⑮側の4本＋3路点滅器側の3本＝9本**が入り，
   接地側2本・**非接地側3本**・渡り線2組の**4か所**になる。刻印は3本の箇所だけ「小」，
   残り3か所が「○」。個数（小4個）は変わらないが根拠が誤りだったので全面的に書き直し，
   誤答肢ロ・ハ・ニの説明と `supplement`（分電盤への矢印も電線2本として数える旨）も修正した。
   ※ 外部解説（1.6 mm×2本が3か所＋1.6 mm×3本が1か所）とも一致することを確認済み。
2. **問41 の写真の名称が誤り**（作成担当の自己申告 (1) が的中）。
   イは「低圧ピンがいし」ではなく **ノップがいし**（造営材に付け上部の溝にIV線をバインドする支持材），
   ロは名称不明としていたが **L形チューブサポート**（ネオン管の固定用）。
   正答ハも「引留クランプ」ではなく **引留がいし（平形がいし）** が正しい呼称。
   `whyCorrect` / 誤答肢イ・ロ / `supplement` を修正した（ニ＝玉がいし・支線の絶縁は元のままで正しい）。
3. **問1 の誤答肢イ・ロ**：示された数値（1.5／1.8）を生まない計算手順が書かれていた。
   具体的な誤答導出の断定をやめ，並列・直列のまとめ方の誤りとして書き直した。
4. **問2 の誤答肢イ**：「6÷8＝0.75 を逆にした」では 43 にならない。
   43 は **6÷(6＋8)＝0.43**（位相差を無視して電流を単純加算した場合）なので，その形に修正。
5. **問38 の誤答肢イ・ロ**：「火災表示灯は照明器具に近い記号」「漏電警報器は ⊿ の中に G」という
   裏の取れない図記号の断定があった。他回（2023下期午後・2024上期）と同じ安全な言い回しに置き換えた。
6. **問29 の `whyCorrect`**：電気工事士法施行規則第2条は「軽微な作業」を定める条文なので，
   「第2条に定める『電気工事士でなければ従事できない作業』に含まれる」は引用の向きが逆。
   「第2条の軽微な作業に挙げられていない」→ 資格が必要，という形に直した。
7. **軽微な整形**：問19 `supplement` の「1.6 mm2本」が mm² と紛らわしいため「1.6 mm×2本」に。
   問50 `whyCorrect` に「写真イは取付枠に『上』表示のある天井取付形で，⑨の天井面のLKコンセントに対応する」旨を追記。

### 確認して問題なしと判断した箇所（作成担当の申告への回答）
- **(2) 問42**：⑫の図記号（黒枠の四角＋丸で囲んだS）＝**電流計付箱開閉器**という読みは，
  本リポジトリの他回（2016下期問49・2022上期午前問49）の記述とも外部解説（「電流計付き開閉器」）とも一致。写真ロ＝電流計付箱開閉器，ニ＝カバー付ナイフスイッチも一致。
- **(3) 問39**：⑨は塗りつぶしのない・壁線に接していないコンセント図記号＋LK傍記で天井面。図で確認。
- **(4) 問43・45・46**：⑮＝4本（2心×2本），⑯＝2本用3個・3本用1個・4本用1個は外部解説と一致。
  「事務所の蛍光灯3灯を2か所の3路点滅器（ア）で点滅」「イ照明＋自動点滅器 A(3A)」も図で確認できた。
- **(5) 問34**：④は手洗場側ジョイントボックスと下側ボックスの間。手洗場側には点滅器ウ・照明ウ・
  プルスイッチ付きの器具がまとまっているので通すのは電源2本のみ。図で確認し公式解答イと整合。
- **(6) 問50**：写真イ＝天井取付の抜け止め形コンセント。⑨と対応することを確認した。
- 問44（イ＝電工ナイフ／ロ＝接地棒／ハ＝リーマ／ニ＝圧着端子）と
  問47（イ＝15A EET／ロ＝20A E／ハ＝250 V用 20A E／ニ＝20A EET）と
  問48（ロ＝ライティングダクト／ニ＝ケーブルラック）と
  問49（イ＝自動点滅器／ロ＝リモコン変圧器／ハ＝3路スイッチ／ニ＝2極スイッチ）も外部解説と一致。

### 残っている懸念
- 問48 の誤答肢ハを「断面の大きい線ぴ・ダクト類」と曖昧に書いている（外部解説では1種金属線ぴ）。
  写真から金属か合成樹脂かを断定できなかったため，あえて曖昧なままにしてある。
- 問32 の `supplement` にある「三相200 Vは対地電圧150 Vを超えるため専用回路・漏電遮断器…」は，
  電技解釈 第143条が本来「住宅の屋内電路」を対象とする規定である点で，やや一般化しすぎている。
  設問の正誤には影響しないので手を入れていない。
- 事務所と手洗場にある「長方形の中に円が2つ」の図記号（傍記 イ／オ）の正式名称を特定できていない。
  問46 の本数の数え方（この方向は白1＋黒1の2本）は公式解答と整合するので実害はない。
