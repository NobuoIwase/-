# denko2-2017-上期 レビューノート

対象: 平成29年度 第二種電気工事士 上期 筆記試験（2017年6月4日実施）／50問
素材: スキャンPDF（`textPdf: false`）。`answerSource: pdf`

## 作業の要点

- **全50問を `cells/qNN_row.png` で目視確認**した（OCR草稿は誤字が多く，そのままでは使えない）。
  問1〜30は row 画像1枚で問いと選択肢を確認。問31〜50は row 画像＋配線図 `pages/p15.png` の部分拡大で確認。
- 解答キーは `answers.json` を使ったが，念のため **解答PDF（`data/raw/denko2/pdf/20170604_co_second_a01.pdf`）を画像化して50問すべて突き合わせ**た。全一致。
  - 途中で「問39（引掛形コンセントの傍記）の公式解答がロ（ET）では？」と誤読しかけたが，解答PDFの原本では **問39＝イ（T）** で，一般的な知識（引掛形＝T）と矛盾しないことを確認した。

## 画像で判断した問（OCRが読めなかった／図がないと解けない）

| 問 | 内容 |
|---|---|
| 1, 2, 4, 5, 6, 8, 10, 27 | 回路図・結線図。`crop_figure.py` で切り出して `figure` に付けた |
| 9 | 選択肢が図（遮断器＋電線太さ＋コンセント）。`choicesFigure` に cells の choices 画像 |
| 16, 17, 18 | 器具・工具・測定器の写真。写真がないと解けない |
| 31〜50 | 配線図（`p15.png`）を `sharedFigure` に。31, 41〜50 は選択肢も画像 |

## 図の切り出しに手を入れた問

スキャン回のため `cells/qNN_figure.png` が生成されないので，全部 `custom/` に手で切り出した。

- `--auto` が外れて y0 を手指定したもの: **問1（0.26）／問6（0.50）／問8（0.36）／問10（0.50）／問27（0.40）**
  （auto は本文中の長い横線を図の上端と誤検出しやすい）
- **問43** は「問い」欄セルが幅14 pxに潰れており（stem/choices の分割位置が壊れている），row 画像から座標指定で
  圧着端子の写真（`q43_fig.png`）を切り出した。
- **問41〜50** は choices セルに問題文の列まで含まれてしまうため，row 画像から罫線位置（x≒420/1647）を検出して
  写真部分だけを `qNN_ch.png` として切り出し，`choicesFigure` に指定した。
- 切り出した画像はすべて Read ツールで開いて目視確認済み。

## 配線図（p15）の読み取りメモ

建物は「1階：宅配ロッカー／ホール／管理室／集会所／エレベータ／共用廊下」，「地下1階：ポンプ室／受水槽室」。
盤は P-L（受電盤・×印だけの長方形），L-1（電灯分電盤），P-1・P-2（ポンプ用の盤・蝶形の記号）。

- ① L-1 の設置位置（破線の四角）／② 地中埋設線 `CV 5.5-2C（PF22）`／③ 外灯の傍記（破線で伏せてある）
- ④ 共用廊下の誘導灯（円に×，向かい合う2つが黒）／⑤ 受水槽室の `◉BL`（表示灯付押しボタン）
- ⑥ P-L 結線図の `Wh`／⑦ 汚水ポンプ（三相200 V）への配線／⑧ ホールJB→宅配ロッカーJB の区間
- ⑨ 宅配ロッカーのコンセントの傍記（破線で伏せてある）／⑩ 引込線取付点
- ⑪ `20A250V E` のコンセント／⑫ 集会所の照明イ（6灯）の中継点（天井内JB）
- ⑬ L-1 結線図の接地線 `IV5.5`／⑭ 湧水ポンプの `◉LF3`／⑮ ポンプ室の `⊠`（プルボックス）
- ⑯ ポンプ室の `(E19)` ねじなし電線管／⑰ ホールのVVF用JB／⑱ 宅配ロッカーのVVF用JB／⑲ ダウンライト `DL`

### 複線図の数え方（問42・47・48）

- **問42（⑫）**: 白5本→中（刻印中）／黒2本→小（刻印○）／帰り4本→小（刻印小） ＝ 中1個＋小2個（○と小）→ ニ。
  「ボックスの位置にある照明も接続に数える」ことで白が5本になり，公式解答ニと一致する。
- **問47（⑰）**: 電源2＋宅配ロッカー2＋ホールの照明2＋3路スイッチ3＋地下4 の計13本。
  白4／黒3／渡り2・2／帰り2 の5か所 ＝ 4本用1・3本用1・2本用3 → ハ。
  「地下1階に至る配線＝渡り線2＋地下照明の白1＋点滅後の線1＝4本」が最少になる。
- **問48（⑱）**: 白5／黒3／帰り4 ＝ 中1個＋小2個 → ロ。コンセントを白・黒に直結する点が要。

いずれも公式解答と独立に複線図を組み立てて一致した。

## category・groupId で迷った問

- **問18（照度計の写真）**: 内容は測定器だが「写真判別」なので分野3（`g3-sokuteiki-shashin`）にした。
  分野5の `g5-sokuteiki-youto`（用途の組合せ）は問24に割り当てて役割を分けている。
- **問34（誘導灯）**: `g6-okugai-tou` の summary にも「誘導灯・非常用照明の区別」とあるが，問33（水銀灯の傍記H100）を
  `g6-okugai-tou` に置いたので，問34は `g6-zumen-kigou-shoumei`（照明器具の図記号）に振った。
- 同じ回で同一 groupId が2問になったもの: `g6-kigou-keiki-bunden`（31・36），`g6-kigou-dendouki-seigyo`（35・44），
  `g6-ringslv`（42・48）。いずれも既存グループの summary に収まる内容なので**新規グループは作らなかった**
  （`denko2-2017-上期.groups.json` は空配列）。

## calc にした問（2問）

- **問3**（電線の抵抗の比較）: `g1-densen-teikou-hikaku`。直径比2倍・答えが2〜6の整数になる組合せだけを `accept` で通す（12通り）。
- **問7**（許容電流と電流減少係数）: 直径2.0 mm は固定にし，本数 `{n}` と減少係数 `{k}` だけをパラメータ化（5通り）。
  - 直径もパラメータにすると `2.0` が文字列化のとき `2` になり「直径2 mm」と表示されてしまうため断念した（**仕様への提案**参照）。
- 図に数値が描かれている問6・問8・問10は，指示どおり calc にせず static のままにした。

## 法改正チェック

- **問29** に `note` を付けた: 現行法では「一般用電気工作物**等**」（＝一般用電気工作物＋小規模事業用電気工作物）。
  2023年3月施行の電気事業法改正に伴う用語変更で，選択肢の正誤は変わらないので `retired` にはしていない。
- 問15（太陽電池）は解説の中で同改正（10 kW以上50 kW未満の太陽電池発電設備＝小規模事業用電気工作物）に触れた。
- そのほかに答えが変わる改正は見当たらず，`retired` は0問。

## 自信が持てなかった箇所（次工程で重点的に確認してほしい）

1. **問7の丸め規則**。公式解答は 2017上期（2.0 mm・0.7）が24 A＝35×0.7＝24.5→24，2015上期（1.6 mm・0.7）が
   19 A＝27×0.7＝18.9→19。単純な切り捨てでも四捨五入でも両方を説明できないため，
   **「小数点以下1位を7捨8入」**（小数部0.8以上のときだけ切り上げ）として解説と `formula` を書いた。
   内線規程の規定表現をもう一度確認してほしい。→ 該当箇所: 問7 `whyCorrect` / `supplement` / `calcTemplate.formula`。
2. **問31の盤の図記号の呼び分け**。「配電盤＝×印だけ／分電盤＝対角線で半分黒／制御盤＝向かい合う三角が黒」と書いた。
   この図面で P-L に×印だけの記号，P-1・P-2（BE＋S を収める盤）に蝶形の記号が使われていることは目視で確認したが，
   JIS の呼称そのものは図面から直接は読めない。JIS C 0303 の原典で確認してほしい。
3. **問45の `⊠`＝プルボックス**。他回（denko2-2022-下期-午前 問36，同じ `⊠` 記号で公式解答＝プルボックス）と
   突き合わせて確定した。ジョイントボックスではない点に注意。
4. **問35の傍記 `BL`**。「押しボタンの記号＋表示灯内蔵」と説明したが，BL の語源・JIS 上の正式な意味づけは未確認。
   公式解答（確認表示灯付の電磁開閉器用押しボタン）とは整合している。
5. **問41のコンセント刃受の向き**。「250 V用は刃受が縦に並び，125 V用は横に並ぶ」と写真から読み取って書いた。
   写真4枚の見分け方としては成立しているが，JIS C 8303 の極配置図で裏取りしてほしい。
6. **問26の零点調整**。「端子間を短絡して零点調整する」と書いた（開放では指針が振れない）。
   接地抵抗計の機種による違いがあるかもしれない。
7. 問22の根拠を「電技解釈 第110条（低圧屋側電線路の施設）」とした。同一敷地内の車庫への供給を屋側電線路として
   扱う整理でよいか，条番号を含めて確認してほしい。

## 仕様への提案（変更はしていない）

- `calcTemplate` のパラメータ置換が `String(value)` なので，`2.0` が `2` と表示されてしまう。
  実試験の表記は「2.0 mm」なので，**パラメータごとに小数桁を指定できる仕組み**（例 `paramDigits: { d: 1 }`）が
  あると，電線の直径（1.6／2.0／2.6）をパラメータ化できて calc の幅が広がる。
  今回は直径を固定して回避した。
- スキャン回では `cells/qNN_stem.png` が幅十数 px に潰れる問がある（この回は問41〜50）。
  `extract_pdf.py` の列分割が「問い」欄と「答え」欄の罫線を見つけられていない。
  罫線検出に失敗したら row をそのまま stem として出す等のフォールバックがあると，`crop_figure.py --from stem` が使える。
- `crop_figure.py --auto` は「本文中の長い横線」を図の上端と誤検出しやすい。
  日本語本文の行（高さ方向に等間隔で黒画素が並ぶ領域）を先に除外すると精度が上がりそう。
