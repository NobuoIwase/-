# denko2-2022-下期-午前 レビューノート

- 素材: テキストPDF（`meta.json` の `textPdf: true`、`answerSource: pdf`）。`questions_raw.json` の本文はおおむね信頼でき、Ω が `W`、mm² の上付き消失、空欄の記号落ちだけを画像で補正した。
- 結果: 50問（active 50 / retired 0）、calc 2問（問3・問8）。`check_answers.py` 欠番なし・不一致なし、`validate-data.ts --exam` errors 0 / warnings 0。

## 画像で確認した問

| 問 | 確認したこと |
|---|---|
| 1, 4, 5, 6, 7, 9 | 回路図・表の数値（Ω が `W` に化けていた箇所を全て補正） |
| 5 | 選択肢が分数表記なので `cells/q05_row.png` で `E/(2R) / E/(√3R) / E/R / 3E/(2R)` を確定 |
| 10 | 選択肢が図（遮断器・電線・コンセントの組合せ）。`q10_row.png` で内容を読み取り、テキスト化したうえで `choicesFigure` も付けた |
| 16, 17, 18 | 写真。16はシースの「EM 600V EEF/F」「TAINEN」表示、17は銘板の「三相200 V 40 μF 放電抵抗器内蔵」、18は3色クリップ付きの検相器 |
| 27 | 選択肢が図（クランプの挟み方）。`q27_row.png` で4通りの挟み方を確定してテキスト化 |
| 29 | 選択肢ハに丸形PSEマークの図が入る。`q29_row.png` で確認 |
| 31〜50 | 配線図面 `pages/p15.png` を PIL で部分拡大して ①〜⑰ の位置・器具・電源方式を確認。41〜50 の写真選択肢は `pages/p12.png`・`p13.png` を拡大して判別 |

## 図の切り出しに手を入れた問

自動切り出し `cells/qNN_figure.png` が本文を含む／図が切れるため、`scripts/crop_figure.py` で作り直して `qNN_fig.png` として参照した。

- 問1（`--y0 0.27`）— 自動だと上端の「2 Ω」ラベルが切れた
- 問4（`--auto`）— 自動の `q04_figure.png` は問題文2行を含んでいた
- 問5、問6（`--auto`）— 問6は回路図と抵抗の表を1枚に収める必要があるため `stem` 全体から切り出した
- 問7（`--y0 0.375`）— 上端の「0.1 Ω」が切れるため2回調整
- 問9（`--y0 0.45`）— 上端の「60 A」が切れるため調整
- 問16・17・18（`--auto`）— 写真部分だけを切り出し

いずれも Read ツールで目視確認済み。問7だけは左上に本文の末尾がわずかに残っているが、図の判読に支障はない。

## 新規グループ（`denko2-2022-下期-午前.groups.json`）

- `g1-sanso-delta-dansen`（三相Δ結線と1線断線）— 既存の `g1-sanso-y-dansen` はY結線の断線が対象で、問5のΔ結線・電源線断線とは解き方（並列になる抵抗の作り方）が違うため分けた。`relatedGroups` で関連付け。
- `g6-zairyou-kougu-kumiawase`（配線図の施工に使う材料と工具の組合せ）— 問50は電線管の接続材料と圧着工具が同じ設問に混在しており、`g6-kanro-setsuzoku-zairyou` にも `g6-setsuzoku-kougu-zairyou` にも収まらない。既存3グループを `relatedGroups` に入れた。

## category / groupId で迷った問

- 問2（ビニル絶縁電線の抵抗と許容電流）— 抵抗の式と許容電流の性質が混在。`g1-denki-teikou-teikouritsu`（分野1）に入れたが、`g2-kyoyou-denryu-gensyou`（分野2）寄りの見方もできる。誤答選択肢が「抵抗の式」中心なので分野1とした。
- 問31（確認表示灯の図記号）— 単独の表示灯なので `g6-zumen-kigou-shoumei`（照明器具の図記号）も候補だが、選択肢が熱線式自動スイッチ・位置表示灯と点滅器系で構成されているため `g6-tenmetsuki-shurui` にした。
- 問41（点滅器の取付けに使う材料）— `g6-bubun-kouji-zairyou`（指示箇所の工事で使用する材料）。`g6-box-shashin` とも迷ったが、問われているのが「取付け工事に使うもの」なので前者にした。
- 問42と問45はどちらもリングスリーブなので同じ `g6-ringslv`。同一日に両方出ないことになるが、仕様どおり「同じ問題の型は同じグループ」を優先した。

## 自信が持てなかった箇所（次工程で重点確認）

1. **問45（⑮のリングスリーブ）と問47（⑰の差込形コネクタ）の解説の内訳**
   公式解答（問45 = 小4個と中2個、問47 = 2本用4個と4本用2個）から逆算した「接続箇所の内訳」を書いている。図面から1本ずつ数え直したところ、⑮は非接地側が6本になる一方で接地側が4本にしかならず、⑰も合計が14心となって公式解答の16心と合わなかった。台所側の器具（レンジフード相当の記号＋プルスイッチ P）へ向かうケーブルの本数と、便所側ジョイントボックスへの送りの本数を読み切れていない可能性が高い。
   そのため解説は「接地側・非接地側がそれぞれ○本以上／渡り線が○か所」という粒度にとどめ、ケーブル1本ごとの心線数の断定は避けた。**複線図を引ける人に、この2問の内訳（特に⑮に集まるケーブルの本数）を検算してほしい。**
   なお問40（⑩＝4本）、問42（⑫＝白4・黒4・戻り2）、問46（⑯＝白1・黒1・3路渡り2）は図面から数えた結果が公式解答と一致しているので、解説は具体的な内訳まで書いている。

2. **問31の「確認表示灯」の図記号の説明**
   ①は点滅器（●）と並ぶ位置に描かれた小さな白丸（○）で、公式解答は確認表示灯。白熱灯も同じ○なので、解説では「大きさとスイッチ列に並んでいる位置」で区別する書き方にした。JIS上の厳密な描き分け（大きさ規定の有無）は未確認。

3. **問44（⑭の器具裏面結線）の誤答理由**
   4枚の写真をグレースケールで拡大して電線の明暗（白＝明るい／赤＝中間／黒＝暗い）と端子位置を読み取り、イ＝負荷側からもコンセントのW側へ渡り線、ロ＝白と赤が入れ替わり、ニ＝黒の渡り線をW端子へ、と判断した。印刷がモノクロで赤と白の区別が難しい箇所があるので、カラー原本での再確認が望ましい。

4. **問50の選択肢イの部材名**
   写真は内側にねじが切られた金属の管接続材料で、止めねじが見当たらないため「ねじ込み式（厚鋼・薄鋼電線管用）」と判断した。カップリングかボックスコネクタかまでは断定できなかったので、解説では「ねじ込み式の管接続材料」という表現にとどめている。

5. **参照条番号**
   自信のあるものだけを書き、曖昧なものは条番号を落として内容だけを書いた（問11・問20・問35・問41 など）。特に問20の「電技解釈 第110条（低圧屋側電線路の施設）」は条番号まで書いたが、念のため確認してほしい。

## 仕様への提案

- **`groups.json` の `g3-zetsuen-saikou-kyoyou-ondo` の summary が誤り**
  現在「IV 60 ℃、HIV・CV・EM-EEF 75 ℃など」となっているが、CV（架橋ポリエチレン）は **90 ℃** で、この回の問12はまさに「CVが最も高い」を答えさせる問題。summary のままだとHIVと同列に見えてしまう。指示により `groups.json` は編集していないので、統合時に「IV・VVF・VVR 60 ℃／HIV・EM-EEF 75 ℃／CV・CVT 90 ℃」へ直してほしい。
- **選択肢に図中の記号が入る問への対応**
  問29の選択肢ハには丸形PSEマークの図が入る。今回は `〔丸形のPSEマーク〕` と文字に置き換えて `note` で断ったが、選択肢1つぶんの画像（`choiceFigures`）を使うか、記号を文字で表す共通ルールを決めておくと回ごとのぶれがなくなる。
- **`calcTemplate.accept` は丸め前の値で評価される**
  `src/engine/calc.ts` は丸める前の `answer` を `accept` に渡している。問3では「1桁で割り切れる組合せだけ採用する」ために `Math.abs(answer*10 - Math.round(answer*10)) < 1e-9` を使ったが、この挙動は `docs/DATA_FORMAT.md` に書かれていない。仕様書に一行足しておくと同じ書き方を再利用しやすい。
- **選択肢の改行**
  問22のように「場所 ─ 工事」の2行構成の選択肢がある。`\n` が描画されるか不明だったため全角ダッシュで1行にまとめた。選択肢での改行の可否を仕様に明記してほしい。
