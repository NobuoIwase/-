# denko2-2020-下期-午前 レビューノート

2020年度（令和2年度）下期 午前 学科試験 50問。素材はスキャンPDF（`meta.json` の `textPdf: false`）。
解答キーは `answers.json`（`answerSource: pdf`）。`check_answers.py` 不一致なし／`validate-data.ts --exam` errors 0・warnings 0。

## 全体

- **全50問を `cells/qNN_row.png` で目視確認**した。OCR草稿（`questions_raw.json`）は誤字が多く（Ω→©/ç、mm²→”、
  「，」と「.」、Hz→日²、kW→×/1 など）、問題文・選択肢はすべて画像から書き起こした。
  特に問41〜50はOCRで問題文と選択肢が入れ替わっており、草稿はほぼ使えなかった。
- 配線図は `pages/p15.png`（木造2階建住宅及び車庫）。①〜⑱の位置は300 dpiで再レンダリングして部分拡大し確認した。
- 分野内訳: 1基礎理論 5／2配電・配線設計 6／3機器材料工具 7／4施工方法 5／5検査 4／6配線図 20／7法令 3 ＝ 50。
- `type: "calc"` は3問（問2・問3・問8）。ほかの計算問題は図中に数値が描かれているため static のままにした
  （問1・問7・問9）。問4・問5・問6は式を選ぶ問題なので static。
- **新規グループは作らなかった**（`data/review/denko2/denko2-2020-下期-午前.groups.json` は不要）。
  既存の156グループですべて割り当てできた。

## 図の切り出し（手作業）

`scripts/crop_figure.py` の `--auto` は問1・問17でうまく働いたが、他は比率指定でやり直した。すべて Read で目視確認済み。

| 問 | 参照名 | 指定 | 内容 |
|---|---|---|---|
| 1 | q01_fig.png | --auto | 直流回路 |
| 4 | q04_fig.png | --y0 0.31 --y1 0.97 | RL直列回路 |
| 6 | q06_fig.png | --y0 0.40 | 三相3線式回路 |
| 7 | q07_fig.png | --y0 0.36 | 単相3線式回路 |
| 9 | q09_fig.png | --y0 0.40 | 幹線と電動機・電熱器 |
| 16 | q16_fig.png | --y0 0.11 | 金属製線ぴの写真 |
| 17 | q17_fig.png | --auto | 電磁開閉器の写真 |
| 18 | q18_fig.png | --y0 0.10 | ホルソの写真 |
| 27 | q27_fig.png | --y0 0.52 | 計器の記号 |

選択肢画像（`choicesFigure`）は、問41〜45は `cells/qNN_choices.png` をそのまま使えたが、
**問46・47・48・49・50はセル分割が崩れており「問い」欄の文章が答え欄の左端に混入していた**ため、
`--from choices --x0 0.235` で左23.5 %を落として `custom/qNN_ans.png` として切り出し直した
（`qNN_choices.png` は `cells/` が優先されるので別名にする必要があった）。

## 判断に迷った点・注意点

- **問28（電気工事士法の主な目的）** — 「法の目的」を問う型に対応する専用グループがなかったので
  `g7-denki-setsubi-gijutsu-kijun`（電気設備技術基準と法令の体系／summary に「根拠法」とある）に入れた。
  他回の同型問題が増えるなら `g7-houreino-mokuteki`（法令の目的）のような新グループに切り出す余地がある。
- **問5（三相電動機の力率の式）** — `denko2-2016-上期` の問5と同一型なので、そこで使われている
  `g1-rikiritsu-keisan` に合わせた。問4（RL直列の力率の式）も同じグループなので、同じ日に両方は出ない。
- **問11** は「地中配線に使用できる電線」だが、`denko2-2018-下期` #12・`denko2-2022-上期-午前` #11 の前例に合わせて
  分野3（材料）・`g3-densen-shurui-youto` にした（分野6の `g6-chichu-densenro` は配線図側）。
- **問26** は `denko2-2017-下期` #26 と同一型のため `g5-setti-zetsuen-kumiawase`（分野5）に合わせた。
- **問42・48（リングスリーブ）は同じ `g6-ringslv`**。1日に1問しか出ない仕様なので、同一回内の重複出題は起きない。

## 自信が持てなかった箇所（次工程で重点的に確認してほしい）

1. **問48（⑱のボックス）の電線の太さの想定** — 中スリーブ2個・小スリーブ2個という公式解答（イ）に合わせるには、
   分電盤（b回路）→⑱ と ⑱→台所方面 の2本がいずれも **VVF2.0** である必要がある。図面上「VVF2.0」の傍記が
   付いているのは分電盤→⑱ の1本だけで、台所側は傍記がない。解説では「台所方面へ送るVVF2.0」と書いたが、
   ここは図面から直接は読み取りきれていない（公式解答から逆算した推定）。
   別解として「白4本＝2.0×2＋1.6×2、黒3本＝2.0×2＋1.6×1」という数え方だけを示す書き方に直してもよい。
2. **問50 選択肢ロの上側の材料の同定** — 内側にねじが切ってあり反対側にねじ首とリングが付いた金属部品を
   「ねじ切り鋼製電線管用のボックスコネクタ」と判断した。この配線図の金属管工事は車庫の (E19)＝ねじなし電線管だけなので
   「ねじなし用でなければならない」という筋で解説を書いている。部品名の断定にはやや不安があるので、
   写真の再確認をお願いしたい（下側のアウトレットボックス自体は使用される材料）。
3. **問44 選択肢イの接点構成図の読み** — 0・1・3の3端子にランプ（○）が組み込まれた図で、
   「位置表示灯内蔵スイッチ（ホタルスイッチ）または確認表示灯内蔵形」とぼかして書いた。
   正解のハ（ランプなしの0・1・3＝3路スイッチ）は確実だが、イの正式名称は断定していない。
4. **問41（⑪の心線数3本の内訳）** — 「3路スイッチ間の渡り線2本＋階段灯へ送る接地側1本」と説明した。
   公式解答（3心1本）とは整合するが、2階側ジョイントボックスの接地側をどこから取るかについては
   図面から一意に読み切れていない。
5. **問40（⑩の工事方法）** — 「木造造営物の低圧屋側電線路に金属管工事は施設できない」という筋で解説した。
   図面冒頭に「木造2階建住宅及び車庫の配線図である」とあることが根拠。既存の
   `denko2-2022-上期-午後` #31 / `denko2-2022-下期-午後` #34 の解説と同じ論法にそろえてある。
6. **問36（⑥の接地抵抗値）** — 500 Ωの根拠は図面11頁の【注意】3「漏電遮断器は，定格感度電流30 mA，
   動作時間0.1秒以内のものを使用している」。この注意書きは `p15.png` には写っていない（11頁にある）ので、
   アプリ上では解説を読まないと判断材料が足りない。**配線図問題の【注意】書きを図面と一緒に見せる仕組み**が
   あるとよい（下記の提案参照）。

## note / retired

- `retired` にした問題はなし。
- `note` を付けたのは**問30のみ**：2023年3月20日施行の電気事業法改正で、小出力発電設備の太陽電池発電設備が
  10 kW未満に狭まり、10 kW以上50 kW未満のものと20 kW未満の風力発電設備が「小規模事業用電気工作物」に移った旨。
  本問の答え（ロ）は現行法でも変わらないので `retired` にはしていない。
- 問21（対地電圧の制限・2 kW以上）、問22・26・36（接地）、問25・35（絶縁抵抗）、問10（分岐回路）、
  問15（遮断器の動作時間）、問29（特定電気用品）は現行法令と照らして変更なしと判断した。

## 計算問題（calc）のパラメータ設計

- **問2**（抵抗の比較）: DA/LA/DB/LB の4パラメータ。`accept` で「答えが整数（許容誤差1e-6）かつ2〜5」に限定。
  1.6×1.6 が 2.5600000000000005 になる浮動小数点誤差があるため、`answer >= 2` ではなく
  `Math.round(answer) >= 2` と書いている（`answer >= 2` だと元問題の組合せ自体が弾かれる）。採用可能10通り。
- **問3**（接触抵抗の発熱量）: R・I・t の3パラメータ、23通り。誤答は「秒に直し忘れ」「時間を掛け忘れ」「I の2乗忘れ」。
- **問8**（許容電流と減少係数）: 断面積Sと許容電流A、本数Nと減少係数k をそれぞれ `accept` で対にした（16通り）。
  丸めは四捨五入ではなく**7捨8入**なので `Math.floor(A * k + 0.2)` としている
  （61×0.7＝42.7 は四捨五入だと43になってしまい実試験の丸め方と食い違う）。

## 仕様への提案

1. **配線図の【注意】書きを問題データ側に持てるようにしたい。** 配線図問題は11頁の
   「屋内配線はVVFによるケーブル工事」「漏電遮断器は30 mA・0.1秒以内」「分電盤の外箱は合成樹脂製」
   といった前提が答えを左右するが、`sharedFigure`（15頁）にはこれが写っていない。
   `sharedFigureNote` のようなフィールド、または図面ページと注意書きページを結合した画像を
   `pages/` に用意する仕組みがあると、配線図20問の解答根拠が自己完結する。
2. **`publish_figures.py` の `qNN_choices.png` の解決順**は `cells/` 固定なので、
   セル分割が崩れた回で切り出し直すと別名（`qNN_ans.png` など）にせざるを得ない。
   `custom/qNN_choices.png` があればそちらを優先する、という順序だと参照名を素直に保てる。
3. **`calcTemplate.accept` に整数判定のヘルパー**（例 `isInt(answer)`）があると、
   浮動小数点誤差を毎回 `Math.abs(answer - Math.round(answer)) < 1e-6` と書かずに済む。
4. `crop_figure.py` に `--from choices` で「左端の問い欄をどこで切るか」を自動推定するオプション
   （縦の罫線を検出する）があると、セル分割が崩れた回の作業が速くなる。
